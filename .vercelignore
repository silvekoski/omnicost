# Keep deploy uploads small — none of these are needed to serve the site or the
# email function. Data is precomputed into the committed JSON blobs.
aiven_usage.parquet
cross_cloud_list_prices.parquet
*.pdf
__pycache__/
*.pyc
.env
