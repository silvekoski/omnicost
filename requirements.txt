# Runtime deps for the Vercel serverless function (api/index.py -> server.py).
# uvicorn is intentionally omitted: Vercel provides the ASGI server.
fastapi>=0.110
httpx>=0.27
resend>=2.0
python-dotenv>=1.0
